import styled from "styled-components";

const StyledApp = styled.div`
  display: flex;
  flex-direction: column;
  width: calc(100% - 10px);
  height: 100%;
  padding: 5px;
`;

export { StyledApp };
